cp ~/.bash_profile .
cp ~/.gitconfig .
cp ~/.gitignore_global .
cp ~/.vimrc .
cp ~/.zshrc .

rm -rf .atom
rm -rf .terminfo
rm -rf .vim
mkdir -p .vim

cp -R ~/.terminfo .
cp -R ~/.vim/ftplugin .vim/

echo ".atom is to be updated manually, skipping."
